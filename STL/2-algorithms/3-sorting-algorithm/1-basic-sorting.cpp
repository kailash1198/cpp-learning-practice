#include <algorithm>
#include <iostream>
#include <iterator>
#include <vector>
using namespace std;

int main(void)
{
    vector<int> listOne = {5, 6, 7, 1, 35};
    vector<int> listTwo = {17, 8, 28, 63, 745};

    // sort(listOne.begin(), listOne.end());
    // vector<int>::iterator a = listOne.begin();
    // while (a != listOne.end())
    // {
    //     cout << *a << endl;
    //     a++;
    // }

    // sort(listOne.begin(), listOne.end());
    // vector<int>::iterator a = listOne.end();
    // while (a != listOne.begin())
    // {
    //     cout << *a << endl;
    //     --a;
    // }
}